@model List<WorkChart>

<table class="table table-hover">
    <tr>
        <th>Öğretmen</th>
        <th>Pazartesi</th>
        <th>Salı</th>
        <th>Çarşamba</th>
        <th>Perşembe</th>
        <th>Cuma</th>
    </tr>
    @foreach (WorkChart item in Model)
    {
        <tr>
            <td>@item.TeacherName</td>
            @foreach (bool work in item.Chart)
            {
                string x = work ? "X" : "";
                <td>@x</td>
            }
        </tr>
    }
</table>